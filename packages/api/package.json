{
  "name": "api",
  "license": "UNLICENSED",
  "main": "src/main.js",
  "scripts": {
    "start": "nodemon src/main.js",
    "start-debugger": "env-cmd --fallback ./.env nodemon --exec \"node --inspect src/main.js\"",
    "test": "jest --verbose --config ./jest.config.json",
    "test-watch": "jest --config ./jest.config.json --watch --coverageReporters",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.17.3"
  }
}
